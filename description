    正常情况下，通过node搭建服务器在监听端口后，可以直接通过“localhost：端口号”来访问发布的页面
    这里之所以可以模拟跨域（直接在地址栏输入域名就跳转），是因为第一：在系统文件hosts文件中，指定
了a.test.com和b.test.com的域名，并且指定两个域名都指向本机（也就是localhost）；第二：我的端口
是80端口，可以省略；综上，所以我能写在一个文件里来模拟跨域（给不同的域名）